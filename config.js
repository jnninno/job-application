// ============================================
// JOB APPLICATION - Configuration
// ============================================
const CONFIG = {
  // Timer settings (in seconds)
  TIMER_DEATH_CORPORATE: 10,     // Consensus mode death timer
  TIMER_DEATH_SHARK: 5,          // Pirate mode shark timer
  TIMER_VICTORY_CHOMP: 3000,     // Victory fake-out duration (ms)
  TIMER_LOADING: 1000,           // Loading screen duration (ms)
  TIMER_VOID_DISSOLVE: 2000,     // Void dissolution effect (ms)
  
  // Developer settings
  IS_DEV: false,                 // Set to true for dev mode (all unlocked)
  SHOW_MEAT_IDS: false          // Set to true to see placeholder IDs
  
  // Future settings (for polished version)
  // ENABLE_TYPEWRITER: true,
  // ENABLE_PARTICLES: true,
  // ENABLE_SCANLINES: true,
  // ENABLE_GLITCH_EFFECTS: true,
  // ENABLE_RGB_VOICES: true
};